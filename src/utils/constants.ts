export const InputTypes = {
  Text: 'text',
  Button: 'button',
  Number: 'number',
};

export const NumberSystem = {
  Bin: 'bin',
  Hex: 'hex',
};

export const NumberSystemRegex = {
  Bin: /^[0-1]+$/,
  Hex: /^[A-Fa-f0-9]+$/,
};
